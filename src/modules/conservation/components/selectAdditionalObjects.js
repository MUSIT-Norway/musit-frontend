//@flow
import React from 'react';
import MusitModal from '../../../components/modal/MusitModal';
import type { MaybeSelectedObject } from '../../../types/conservation';

type Props = {
  objects: Array<MaybeSelectedObject>
};

const SelectObjects = (props: Props) => (
  <MusitModal>
    <div />
  </MusitModal>
);
